<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rief"
    >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_gituser"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_gituser"
        android:supportsRtl="true"
        android:theme="@style/Theme.AplikasiGithubUser">
        <activity
            android:name=".github.view.activity.SplashActivity"
            android:theme="@style/Theme.SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".github.view.activity.DetailUserActivity"
            android:exported="false"
            />
        <activity
            android:name=".github.view.activity.MainActivity"
            android:exported="false">
        </activity>
    </application>

</manifest>